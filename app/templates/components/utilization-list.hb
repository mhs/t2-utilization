<div class="staffList">
  <div class="number-container">
    <span class="numbers">{{property.length}}</span>
  </div>
{{title}}
</div>

<ul>
  {{#each property}}
  <li>{{name}}</li>
  {{/each}}
</ul>
